package sesion03.actividades;

public class fracmentos {
  
}
/*
int contador = 0;
for (Habitacion habitacion : listaHabitaciones) {
  if (habitacion instanceof HabIndividual) {
    contador++;
    habitacion.toString();
  } else if (habitacion instanceof HabDoble) {
    HabDoble habDob = (HabDoble) habitacion;
  // Acceder a métodos y atributos específicos de HabDoble
  } else if (habitacion instanceof HabTriple) {
    HabTriple habTri = (HabTriple) habitacion;
  // Acceder a métodos y atributos específicos de HabTriple
  } else if (habitacion instanceof HabFamiliar) {
    HabFamiliar habFam = (HabFamiliar) habitacion;
  // Acceder a métodos y atributos específicos de HabFamiliar
  }
}

*/